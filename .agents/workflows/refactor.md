---
description: リファクタリングワークフロー。テストを維持しながら段階的にコードを改善する。
---

# リファクタリングワークフロー

## 推奨モデル
| リファクタの規模 | 推奨モデル |
|---|---|
| 単一ファイル | Gemini 3.1 Pro (Low) |
| 複数ファイル横断 | Gemini 3.1 Pro (High) |

## 前提条件
- `.agents/context/CODEBASE_MAP.md` を読み、対象ファイルの依存関係を把握する
- `.agents/context/ARCHITECTURE.md` を読み、リファクタリング後のあるべき姿を理解する
- `PROJECT_CONSTITUTION.md` §8.1（TDDサイクル）の Refactor ステップに準拠する

## ワークフロー

### Step 1: リファクタリングの目的確認
ユーザーからの指示を以下に整理する:

```
- 対象: （ファイル名 or モジュール名）
- 目的: （可読性向上 / パフォーマンス改善 / 重複排除 / 責務分離 等）
- 制約: （外部インターフェースの変更可否）
```

### Step 2: 現状分析
// turbo
1. 対象ファイルを読み、現在の構造・問題点を分析する
2. 依存関係を特定する（何がこのファイルをimportしているか）
// turbo
3. 既存テストの有無と内容を確認する

### Step 3: リファクタリング計画
以下を策定してユーザーに提示する:
- 変更前後の構造比較
- 影響を受けるファイル
- リスク評価

### Step 4: 既存テストでの基準設定
// turbo
1. `npm test` を実行し、現在のテスト結果を記録する
2. テストがない場合は、リファクタリング前に回帰テストを追加する

### Step 5: 段階的リファクタリング
小さな単位で変更し、各ステップでテストを実行する:

1. 変更を1つ行う
// turbo
2. `npm test` で全テスト通過を確認する
3. 繰り返す

### Step 6: 最終検証
// turbo
1. `npm run build` が成功することを確認する
// turbo
2. `npm test` で全テストスイートが通過することを確認する
3. リファクタリング前と同等の機能が維持されていることを確認する

### Step 7: ドキュメント同期（必須）
以下のファイルを確認し、コード変更に伴う更新が必要かチェックする:
1. `.agents/context/CODEBASE_MAP.md` — ファイル分割/統合があった場合は更新
2. `.agents/context/ARCHITECTURE.md` — アーキテクチャパターンの変更があった場合は更新

### Step 8: 完了報告
```
## リファクタリング完了
- 対象: ...
- 変更内容: ...
- 改善効果: ...
- テスト: ✅ 全パス
- ドキュメント更新: ✅ / 不要
```
